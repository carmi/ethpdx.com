function private addSlot(definition)(string memory definition) private {
    address
  }

  function private createNFT(address addr) {
      console.log(msg.sender,"TODO: nft generated for addr: ", addr);
  
